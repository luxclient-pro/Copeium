package cope.client.modules;

import cope.client.Cope;

public class DisconnectScreenPlus extends BetterModule {
    public DisconnectScreenPlus() {
        super(Cope.CATEGORY, "disconnect-screen-plus", "Puts a delete button on the disconnect screen.");
    }
}
